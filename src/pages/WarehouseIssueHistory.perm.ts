import type { PageDef } from '../lib/permissionsCatalog';

const def: PageDef = {
  key: 'view_warehouse_issue_history',
  label: 'Issue History',
  to: '/warehouse/issue-history',
  group: 'warehouse',
  order: 8,
};

export default def;
