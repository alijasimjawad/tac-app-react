import type { PageDef } from '../lib/permissionsCatalog';

const def: PageDef = {
  key: 'view_warehouse_inventory',
  label: 'Inventory',
  to: '/warehouse/inventory',
  group: 'warehouse',
  order: 2,
};

export default def;
